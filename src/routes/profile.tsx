import { createFileRoute, useNavigate } from "@tanstack/react-router";
import { Award, Bell, ChevronRight, Coffee, Compass, GraduationCap, Medal, Settings, Sunrise } from "lucide-react";
import profileImage from "../assets/seatify-hero.jpg";
import { useSeatify } from "../lib/seatify-context";

export const Route = createFileRoute("/profile")({ head: () => ({ meta: [{ title: "Student Profile — Seatify" },{ name: "description", content: "Manage your Seatify student profile and preferences." },{ property: "og:title", content: "Student Profile — Seatify" },{ property: "og:description", content: "Manage your Seatify profile." },{ property: "og:type", content: "website" },{ name: "twitter:card", content: "summary_large_image" }] }), component: Profile });

function Profile() {
  const { replayOnboarding } = useSeatify();
  const navigate = useNavigate();
  const replay = () => { replayOnboarding(); navigate({ to: "/" }); };
  return <div className="mx-auto max-w-4xl px-5 py-10 lg:py-16"><p className="text-xs font-semibold uppercase tracking-[0.18em] text-muted-foreground">Your Seatify</p><h1 className="mt-2 font-display text-4xl font-semibold sm:text-5xl">Profile</h1><section className="mt-8 overflow-hidden rounded-xl bg-primary text-primary-foreground"><div className="flex flex-col items-center gap-5 p-7 text-center sm:flex-row sm:p-9 sm:text-left"><img src={profileImage} alt="Student profile" width={1536} height={1024} className="size-24 rounded-full border-4 border-primary-foreground/15 object-cover" /><div className="flex-1"><h2 className="font-display text-3xl font-semibold">Student</h2><p className="mt-1 flex items-center justify-center gap-1.5 text-sm opacity-70 sm:justify-start"><GraduationCap className="size-4" />Universitas Indonesia</p></div><span className="rounded-full border border-primary-foreground/20 px-4 py-2 text-xs font-semibold">Campus Explorer</span></div><div className="grid grid-cols-3 border-t border-primary-foreground/15 text-center"><div className="p-5"><strong className="font-display text-3xl">12</strong><span className="mt-1 block text-xs opacity-60">Cafes visited</span></div><div className="border-x border-primary-foreground/15 p-5"><strong className="font-display text-3xl">36h</strong><span className="mt-1 block text-xs opacity-60">Study hours</span></div><div className="p-5"><strong className="font-display text-xl sm:text-2xl">Quiet</strong><span className="mt-1 block text-xs opacity-60">Favorite vibe</span></div></div></section><section className="mt-10"><p className="text-xs font-semibold uppercase tracking-[0.18em] text-muted-foreground">Achievements</p><div className="mt-4 grid gap-3 sm:grid-cols-3">{[{i:Compass,t:"Campus Explorer",d:"Visited 10 campus cafes"},{i:Sunrise,t:"Early Bird",d:"5 check-ins before 9 AM"},{i:Coffee,t:"Coffee Hunter",d:"Tried 8 hidden gems"}].map(({i:Icon,t,d}) => <div key={t} className="rounded-lg border border-border bg-card p-5"><span className="grid size-11 place-items-center rounded-full bg-accent"><Icon className="size-5 text-accent-foreground" /></span><h3 className="mt-4 font-display text-xl font-semibold">{t}</h3><p className="mt-1 text-xs text-muted-foreground">{d}</p></div>)}</div></section><section className="mt-10"><p className="text-xs font-semibold uppercase tracking-[0.18em] text-muted-foreground">Preferences</p><div className="mt-4 divide-y divide-border rounded-lg border border-border bg-card">{[{i:Bell,t:"Availability alerts"},{i:Settings,t:"Account settings"}].map(({i:Icon,t}) => <button key={t} className="flex w-full items-center gap-3 p-5 text-left transition-colors hover:bg-secondary"><Icon className="size-5 text-primary" /><span className="flex-1 text-sm font-medium">{t}</span><ChevronRight className="size-4 text-muted-foreground" /></button>)}<button onClick={replay} className="flex w-full items-center gap-3 p-5 text-left transition-colors hover:bg-secondary"><Award className="size-5 text-primary" /><span className="flex-1 text-sm font-medium">Replay app introduction</span><ChevronRight className="size-4 text-muted-foreground" /></button></div></section></div>;
}